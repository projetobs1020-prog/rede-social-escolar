window.Comments={toggle(id){const el=document.getElementById("comments-"+id),p=Store.posts().find(x=>x.id===id);el.innerHTML=`<div class="comments">${p.comments.map(c=>`<div class="comment"><b>${App.escape(c.name)}</b>${App.escape(c.text)}</div>`).join("")}<div class="comment-form"><input id="comment-${id}" placeholder="Escreva um comentário..."><button class="btn primary" onclick="Comments.add(${id})">Enviar</button></div></div>`},add(id){const input=document.getElementById("comment-"+id),t=input.value.trim();if(!t)return;const all=Store.posts(),p=all.find(x=>x.id===id);p.comments.push({name:Auth.user.name,text:t});Store.savePosts(all);Posts.list();Comments.toggle(id);App.toast("Comentário publicado.")}};